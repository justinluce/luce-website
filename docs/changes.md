## 2026-06-05 — Re-enable Writing section

- `src/App.jsx` — restored `Writing` and `BeyondTheRealm` imports plus their `/writing` and `/writing/beyond-the-realm` routes (reverting commit b7a1ef3).
- `src/shared/components/Sidebar.jsx` — restored the `WritingIcon` and the Writing nav item.
